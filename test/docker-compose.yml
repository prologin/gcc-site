version: '3'
services:
  #postgres:
  #  image: postgres:12
  #  volumes:
  #  - ./db-data:/var/lib/postgresql/data
  #  hostname: postgres
  #  environment:
  #  # NOT FOR PRODUCTION
  #  - POSTGRES_PASSWORD=mysecretpassword


 #web: # front + reverse proxy
 #  build:
 #    context: ../front
 #  volumes:
 #  - ./nginx.conf:/etc/nginx/conf.d/default.conf
 #  ports:
 #  - 8000:80

  api:
    build:
      context: ../api
    ports:
    - 8000:8000
